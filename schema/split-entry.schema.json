{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://cdn.jsdelivr.net/gh/coffee-cpu/stock-splits-data@main/schema/split-entry.schema.json",
  "title": "Stock Split Entry",
  "description": "Schema for a single stock split entry in yearly files",
  "type": "object",
  "required": ["symbol", "name", "date", "ratioNew", "ratioOld"],
  "properties": {
    "symbol": {
      "type": "string",
      "description": "Stock ticker symbol",
      "pattern": "^[A-Z0-9.]+$"
    },
    "name": {
      "type": "string",
      "description": "Company name"
    },
    "date": {
      "type": "string",
      "format": "date",
      "description": "Split effective date (YYYY-MM-DD)",
      "pattern": "^\\d{4}-\\d{2}-\\d{2}$"
    },
    "ratioNew": {
      "type": "integer",
      "description": "New shares received in the split (e.g., 4 in a 4-for-1 split)",
      "minimum": 1
    },
    "ratioOld": {
      "type": "integer",
      "description": "Old shares exchanged in the split (e.g., 1 in a 4-for-1 split)",
      "minimum": 1
    },
    "isin": {
      "type": "string",
      "description": "International Securities Identification Number",
      "pattern": "^[A-Z]{2}[A-Z0-9]{10}$"
    },
    "exchange": {
      "type": "string",
      "description": "Primary exchange (e.g., NASDAQ, NYSE)"
    },
    "source": {
      "type": "string",
      "description": "Data source URL or reference"
    },
"notes": {
      "type": "string",
      "description": "Additional notes about the split"
    }
  },
  "additionalProperties": false
}
